[project]
name = "{{project_lower}}"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">={{python}}"
# 依存の pin には理由をコメントで残す (例: 上流の API 変更で壊れる, など)
dependencies = [
    "hydra-core>=1.3,<1.4",
    "matplotlib",
    "numpy",
    "omegaconf>=2.3",
    "pytorch-lightning>=2.0",
    "torch>=2.0",
    "wandb",
]

[dependency-groups]
dev = [
    "ipdb",
    "pytest",
]
